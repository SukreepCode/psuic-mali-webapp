export const JWT_LOCAL_STORAGE_KEY = 'jwtToken';

/**
 * This will be used to set all HTTP request
 * This key must be match with the server
 * 
 * The server will read the JWT TOKEN from this key
 */
export const HTTP_HEADER_AUTHORIZATION_KEY = 'Authorization';